apiVersion: v1
kind: Service
metadata:
  name: {{ include "aws-dev.fullname" . }}
  labels:
    {{- include "aws-dev.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aws-dev.selectorLabels" . | nindent 4 }}
