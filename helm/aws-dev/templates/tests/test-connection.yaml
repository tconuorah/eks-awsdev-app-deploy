apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "aws-dev.fullname" . }}-test-connection"
  labels:
    {{- include "aws-dev.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "aws-dev.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
